name: 🚀 Deploy All AutonomaX Services to Cloud Run

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
  REGION: ${{ secrets.GCP_REGION }}

jobs:
  deploy-syncshopify:
    name: 🛒 Deploy syncShopify Service
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Google Cloud SDK
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ secrets.GCP_PROJECT_ID }}
          service_account_key: ${{ secrets.GCP_SA_KEY }}
          export_default_credentials: true

      - name: Deploy to Cloud Run - syncShopify
        uses: google-github-actions/deploy-cloudrun@v2
        with:
          service: syncshopify-service
          region: ${{ env.REGION }}
          source: ./cloudfunctions/syncShopify

  deploy-strategyloop:
    name: 🧠 Deploy strategyLoop Service
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Google Cloud SDK
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ secrets.GCP_PROJECT_ID }}
          service_account_key: ${{ secrets.GCP_SA_KEY }}
          export_default_credentials: true

      - name: Deploy to Cloud Run - strategyLoop
        uses: google-github-actions/deploy-cloudrun@v2
        with:
          service: strategyloop-service
          region: ${{ env.REGION }}
          source: ./cloudfunctions/strategyLoop

  deploy-fiverrsync:
    name: 🎯 Deploy fiverrSync Service
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Google Cloud SDK
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ secrets.GCP_PROJECT_ID }}
          service_account_key: ${{ secrets.GCP_SA_KEY }}
          export_default_credentials: true

      - name: Deploy to Cloud Run - fiverrSync
        uses: google-github-actions/deploy-cloudrun@v2
        with:
          service: fiverrsync-service
          region: ${{ env.REGION }}
          source: ./cloudfunctions/fiverrSync